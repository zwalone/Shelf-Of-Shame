import React from 'react'
import { View, Text } from 'react-native'

export default Games = ({route, navigation}) => {
    return (
        <View>
            <Text>Login</Text>
        </View>
    )
}